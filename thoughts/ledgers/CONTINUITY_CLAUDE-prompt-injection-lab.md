# 継続台帳 — prompt-injection-lab (Phase 1)

## Goal
`DESIGN.md` / `IMPLEMENTATION_PLAN.md` に基づく Phase 1 の実装．DoD は §0：
測定器の信頼性開示・単一設定 ASR の否定（union coverage）・多試行 ASR と信頼区間・
ベンチマーク間の非独立性（ContentKey）・過剰拒否・三値判定の保持・network-free 既定ビルド，
がすべて CLI から再現可能であること．

## Constraints
- 言語 Rust，crate 接頭辞 `pil-`，ハイブリッド型モノレポ（自作は単一 repo，上流は `third_party/` submodule で SHA 固定）
- 既定ビルドは **network-free**．`ollama`/`openai`/`anthropic`/`gemini` を cargo feature で gate
- 三値判定 `Success | Failure | Undecidable{reason}` を全経路で保持
- `CaseId`（identity, source 含む）と `ContentKey`（dedup, source 含めない）を分離
- 上流 Python ローダは使わず native Rust で submodule 実ファイルを直読
- 有害データ・データ実体を vendoring しない（submodule 参照のみ）

## Key Decisions
- AdvBench は原典 `llm-attacks` を正とする（§3.2）
- StrongREJECT は v1(alexandrasouly)/v2(dsbowen) 両実装（§3.7）
- fine-tuned judge は Phase 2 送り（§8.3）

## State
- Done:
  - [x] M0 workspace 基盤（Cargo workspace / submodule 全固定 SHA / CI / toolchain）— build/test/clippy/fmt 緑
  - [x] M1 pil-core（型定義）— 17 tests 緑，CaseId/ContentKey 性質・serde ラウンドトリップ
  - [x] M2 pil-metrics::reliability — 4 tests 緑，§3.1 完全再現（FPR 0.268 等・kappa・一致率外れ値）
  - [x] M3 pil-metrics::instrument — 46 tests（string_match/rubric v1v2/harmbench_cls/minhash，三値 Undecidable 全経路，Judge 抽象で network-free）
  - [x] M4 pil-llm — 12 tests，§6.2 cache key（framed），seed=base.wrapping_add(attempt)，mock 既定，ollama feature ビルド緑
  - [x] M5 pil-bench-* — 21 tests，件数実測一致，§9.1 落とし穴回帰済み
  - [x] M6 pil-attacks — 14 tests，決定論レンダラ + provenance + default_variants()
  - 統合: workspace 全体 110 tests 緑 / clippy クリーン / fmt クリーン
  - [x] M8 pil-metrics::aggregate — pil-metrics 60 tests．Wilson/CP/bootstrap(percentile+BCa) テストベクタ一致，union coverage(single<union)，EnvKind/InstrumentReff 跨ぎは CrossEnv/CrossInstrument マーカー(private field)で opt-in 強制，Undecidable 分母除外+件数保持
  - [x] M7 pil-runner — 24 tests．中断再開の冪等性実証（resume で完了タプル再生成 0），tuple=(CaseId,instrument,attempt,seed,attack)，backoff+token-bucket 単体テスト，1生成に複数 instrument
  - [x] M9 pil-report — 16 tests．**§3.4 を ContentKey で完全再現（11/9/0，正規化でも乖離なし）**，二値化+undecidable 併記，ASR+CI 併記，reliability 整形
  - [x] M10 pil-cli — 11 tests．reliability/run/report 3サブコマンド，suites/*.toml，results/{sub}_TS/ + provenance.json
  - [x] **M11 E2E** — CLI 実行で §0 DoD 全項目を再現確認（下記）
- **Phase 1 完了 ✅** — workspace 全体 **175 tests 緑 / clippy 0 / fmt クリーン / ollama feature ビルド緑 / reqwest 既定非依存**
- Remaining: なし（次は Phase 2：環境型・横断比較・OpenAI 互換シム・fine-tuned judge）

## M11 E2E 実証（CLI 実行で確認済み）
- `pil reliability`: §3.1 完全再現（harmbench_cf FPR 0.268・水増し 1.34x・kappa 0.809/0.826/0.886・一致率外れ値 77-78%）
- `pil run --suite suites/phase1-smoke.toml --provider mock`: 360 trials，checkpoint/provenance 出力
- `pil report`: EnvKind=StaticPrompt 単一を明示（§10 跨がない）／単発 ASR+Wilson CI+undecidable 併記／**union 単一最良 72.2% < union 100%**／asr@k 曲線／過剰拒否／**非独立性 11・9・0**

## 次セッション申し送り
- **README.md 整備済み（175行）** — DESIGN.md の要点を反映（概要/設計原則/11 crate 表/セットアップ/ビルド/CLI 3サブコマンド/責任ある利用/Phase 計画/MIT）．CLI フラグ実測一致・禁止句読点0・「較正」0
- **DESIGN.md は Phase 2 後まで残す（ユーザー決定）** — Phase 2 が §4.1 制御反転/OpenAI互換シム・§8.3 fine-tuned judge の詳細に依拠するため．Phase 2 完了時に README が全て覆ってから削除
- scratch_*.sh / scratch_*.log は gitignore 済み（掃除可）

## M3-M6 が公開した主要 API（統合の手がかり）
- M6 `pil_attacks::render(&Case, &AttackRef) -> Result<String, RenderError>`, `default_variants()`
- M4 `pil_llm`: `LlmProvider` trait(object-safe, BoxFuture), `GenerateRequest/GenerateOutput`, `CachingClient` + `cache_key`, `CacheEntry`(serde), `MockProvider`, `LlmConfig`, `seed_for_attempt(base, attempt)`
- M3 `pil_metrics::instrument`: `Instrument` trait(`measure(&Case,&Response)->Measurement`), `Judge` trait(`Fn(Option<&str>,&str)->Result<String,JudgeError>`), 各 instrument 構造体
- M5 `pil_bench_*`: `load*(repo_root) -> Result<Vec<Case>, _>`, JBB `load_judge_comparison`

## Confirmed facts (実測)
- §3.1 数値は Python で先行検証済み（回帰目標値確定）
- dsbowen/strong_reject = MIT (c) 2024 Dillon Bowen, 最終コミット 2025-07-07
- submodule 実データ配置: JBB `third_party/JBB-Behaviors/data/`, AdvBench `third_party/llm-attacks/data/advbench/harmful_behaviors.csv`, HarmBench `third_party/HarmBench/data/behavior_datasets/` + `data/copyright_classifier_hashes/*.pkl`, SR `third_party/strongreject/strongreject_dataset/`
- pil-core 公開 API: SourceRef / Case(new) / CaseId::derive / ContentKey::derive / EnvKind / Verdict / UndecidableReason / InstrumentRef / MeasurementParams / Measurement / ModelRef / Response / FinishReason / Trial / AttackRef / Transform / normalize

## Open Questions
- UNCONFIRMED: `dsbowen/strong_reject` のライセンス・最終コミット日（M0 で確定，§7.1 暫定行）
- UNCONFIRMED: submodule 取得にネットワークが使えるか（環境依存）

## Working Set
- ブランチ: main（コミットなしから開始）
- テスト: `cargo test`（既定 feature = network-free）/ `cargo clippy -D warnings` / `cargo fmt --check`
- 参照: DESIGN.md / IMPLEMENTATION_PLAN.md

---
*This file was generated by Claude Code.*
