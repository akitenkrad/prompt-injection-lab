# 継続台帳 — prompt-injection-lab (Phase 2)

## Goal
DESIGN §10 Phase 2「環境型と横断比較」の実装．DoD は `thoughts/plans/phase2-plan.md` §0：
OpenAI 互換シム・sidecar adapter・環境種別跨ぎ比較の成立・fine-tuned judge・network-free 既定の維持，
がすべて CLI から再現可能であること．

## Constraints
- Phase 1 の制約を継承（Rust / `pil-` 接頭辞 / 三値判定 / CaseId vs ContentKey / native 直読 / 非 vendoring）
- **network-free 既定を維持**：シム・sidecar・実 LLM は cargo feature で gate．既定テストは loopback + MockProvider + スタブ Python
- **制御反転（§4.1）**：モデル呼び出しは全て `pil-llm` の単一経路に集約．Python は irreducible な環境/ツール本体のみ
- **native-first（§4.1）**：「Rust に移すと測定値が変わりうるか」で Python 温存/Rust 移設を判断
- Phase 1 の 175 tests 回帰を壊さない

## Key Decisions
- HTTP サーバは axum（feature `shim`）を推奨．確定は P2-M1 実装時
- fine-tuned judge の GGUF 化は事前手順，Rust 側は logprob 期待値式のみ（§8.3）
- DESIGN.md は Phase 2 完了まで残す（詳細設計を参照するため．ユーザー決定）

## State
- Done:
  - [x] Phase 1 完了（175 tests / clippy 0 / fmt clean）→ commit 876b021
  - [x] README.md 整備（DESIGN 要点反映）
  - [x] Phase 2 詳細計画作成（`thoughts/plans/phase2-plan.md`）
  - [x] **P2-M1 `pil-shim`（OpenAI 互換ローカルエンドポイント，keystone §4.1）** — 独立検証済み：default **184 tests**（175+9 mapping）/ `--features shim` **12 tests**（3 loopback は 127.0.0.1:0 で MockProvider 到達確認）/ **axum は既定ビルド非依存（tree 0 行）** / clippy 0（default+shim）/ fmt clean．feature `shim`（default=[]），`/v1/chat/completions`+`/v1/models`，OpenAI↔GenerateRequest 純関数 mapping は feature 非依存
- Now: [→] P2-M2 tool-calling スキーマ変換（OpenAI ↔ Anthropic/Gemini）
- Next: P2-M3 `pil-sidecar` 基盤
- Remaining:
  - [ ] P2-M3 `pil-sidecar` 基盤（native-first）
  - [ ] P2-M4 AgentDojo 取り込み（EnvKind::Emulated 初の実データ）
  - [ ] P2-M5 fine-tuned judge（qylu4156/strongreject-15k-v1，logit 期待値式）
  - [ ] P2-M6 横断比較レポート（cross-EnvKind，Kendall's W）
  - [ ] P2-M7 E2E（Phase 2 総合）

## pil-llm 公開 API（シムが包む単一経路）
- `LlmProvider` trait（object-safe, `generate(&GenerateRequest) -> BoxFuture<Result<GenerateOutput, LlmError>>`）
- `GenerateRequest { model: ModelRef, prompt: String, config: LlmConfig, attempt: u32, top_logprobs: Option<u32> }`，`effective_seed()`
- `GenerateOutput { response: Response, metadata: CallMetadata, logprobs: Option<Vec<TokenLogprobs>> }`
- `TokenLogprobs { token, logprob, top: Vec<TopLogprob> }`（§6.3 fine-tuned judge が参照）
- `MockProvider`（network-free テスト用），`CachingClient` + `cache_key`，`LlmConfig`，`seed_for_attempt`
- `Box<T>`/`Arc<T>` にも `LlmProvider` 実装済み（`Arc<dyn LlmProvider>` でシムに注入可）

## Open Questions
- UNCONFIRMED: Ollama OpenAI 互換経路での logprobs 実返却（native は確定．P2-M1/M5 で実測）
- UNCONFIRMED: AgentDojo の Python 依存をどこまで Rust に剥がせるか（コード精読で個別判定）
- UNCONFIRMED: LoRA merge→GGUF が Ollama で top_logprobs を返すか（駄目なら vLLM sidecar 退避）

## Working Set
- ブランチ: main（Phase 1 = 876b021）
- テスト: `cargo test`（既定 network-free）/ `cargo clippy -D warnings` / `cargo fmt --check`
- シム有効化: `cargo build --features pil-shim/shim`
- 参照: DESIGN.md §4.1/§4.2/§8.3 / `thoughts/plans/phase2-plan.md` / README.md

---
*This file was generated by Claude Code.*
